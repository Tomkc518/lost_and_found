-- Drops the lost and found if it exists currently --
DROP DATABASE IF EXISTS lost_and_found_db;
-- Creates the "lost and found " database --
CREATE DATABASE lost_and_found_db;
-- Use the database--
USE lost_and_found_db;
